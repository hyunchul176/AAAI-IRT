# AAAI 논문 프로젝트 — AV 적대 평가의 SUT-불변 측정 (IRT)

> **[항상 적용] 이 프로젝트에서는 코딩 작업이 아닌 한, 아래 "voice" 규칙을 기본값으로 따른다.**
> 흐르는 산문으로 쓰고, 통계·기술 용어는 처음 나올 때 쉬운 말·비유로 풀며, 표·배지·불릿을 반사적으로 쓰지 않는다.
> 결론만이 아니라 이유와 맥락을 설명한다.

이 디렉토리는 코드 프로젝트가 아니라 **학술 논문(AAAI 메인 트랙) 연구 기획·원고 작업** 공간이다.
주요 산출물은 `research/`의 HTML 정리 사이트(연구 개요·방법 형식화·문헌 검토·질의 로그)와 원고 텍스트이며,
원본 계획 문서는 `AAAI_SUT_invariant_measurement_plan.md`다.

## 답변·문서의 voice (이 프로젝트에서 가장 중요)

- **흐르는 산문으로 쓴다.** 불릿·표·배지를 반사적으로 쏟아내지 말고, 잘 전개된 문단으로 설명한다.
  비교가 꼭 필요할 때만 표를 쓴다.
- **이유와 맥락을 설명한다.** 결론만 던지지 않는다. 나는 도메인 연구자지만 측정통계(IRT 등)의 전문가는 아니므로,
  통계·기술 용어는 **처음 나올 때 쉬운 말이나 비유로 풀어** 준다.
- **시험 비유를 일관되게 쓴다**: 문항 = 시나리오, 수험생 = AV 정책, 오답 = 충돌, 문항 난이도 = 시나리오 위험도,
  수험생 실력 = AV 강건성.
- **국문 학술 문체**: 도입은 소절 없이 단일 흐름, em-dash(—) 금지, 직역체·명사 나열 회피, 기술용어는 영어 병기.

### 쓰지 않는 표현·요소 (사용자 지적 누적 — 앞으로도 금지)

- **UI 장식 금지**: 배지(badge, 예: DRAFTED/PENDING, "정독 가능 N편"), 진행상태 라벨("정독 자료 확보", "정독 보유", "자료 확보 상태"), 반사적 표·불릿.
- **메타포·뭉뚱그리는 말 금지**: 부품, 묶음, 척추, 사다리, 축, 무게중심, 주축, 토대(→바탕·이론·기초), 바닥(→하한), 손잡이(→조절 방식).
- **em-dash(—) 금지(재확인)**: 구분이 필요하면 콜론(:)이나 가운뎃점(·)을 쓴다. 범위 표시 en-dash(–)는 허용.
- **제목은 내용이 한눈에 보이게**: 섹션·카드 제목에 추상 명사만 나열하지 말고, 읽으면 무슨 내용인지 바로 파악되게 구체적으로 쓴다.
- **라벨식 도식 금지**: "현상/쟁점/처리"처럼 항목을 라벨로 찍는 구조. 흐르는 산문이나 자연스러운 설명으로 푼다.
- **불필요한 메타 설명 금지**: "이 리뷰는 원고용 요약이 아니라~"처럼 무엇이 아닌지 설명하는 군더더기.
- **자연스러운 동사**: "위험도를 잰다"가 아니라 "측정한다". 어색한 합성어·명사 나열을 피한다.
- **구어·과장 표현 금지**: "자백/자인", "선구자", "그냥 빼며·그냥 ~버린다", "손으로 고른", "세 자백". 대신 학술 문체로 — "인정한다·스스로 밝힌다", "가장 가까운 선행 연구", "평가에서 제외한다", "직접 정한·수작업으로 정의한", "세 가지 한계".
- **인용**: abstract와 Introduction에서 따오지 않는다. 본문(Results·Discussion·Conclusion)에서 발췌하고 2차 인용 여부를 확인한다.

### 정독 카드 작성 규칙

- **figure는 PDF에서 추출**한다(직접 캡처 아님): `pdftoppm -r 200`으로 해당 페이지를 PNG 렌더 → 내가 이미지를 직접 보고 위치 확인 → `magick ... -crop`으로 잘라 `research/review/images/`에 저장 → 다시 보고 확인. 논문마다 **주요 figure·table을 누락 없이** 포함한다.
- **파트마다 하이라이트**: 정독 카드의 각 파트(연구 배경·무엇을 했나·방법론 상세·결과·한계·본 연구와의 관계)에 꼭 읽어야 할 핵심 문장 한 곳을 `<span class="mark">`로 표시한다. 어느 파트도 빠뜨리지 않는다.
- **모든 페이지에 다크모드 토글**(`.theme-toggle` 버튼)을 넣는다.

## 사실성 원칙

- 인용·수치·인용문·결과를 지어내지 않는다. 확인되지 않은 것은 "확인 필요"로 명시한다.
- 특정 논문의 내용은 **전문(full paper) PDF**로 검증한다. leapspace(ScienceDirect LLM)는 광역 서베이 전용이며,
  특정 논문을 파고드는 용도가 아니다.

## 협업 도구 역할

- **leapspace** — 코퍼스 전반 광역 조사. 질문은 500자 이내 영문, 사용자가 직접 받아온다.
- **PDF 전문 정독** — 특정 논문의 표·수치·인용문이 필요하면 full paper를 `pdfs/`에 받아 직접 정독한다.
- **오픈웹/arXiv** — Claude가 직접 검색·정독한다.
